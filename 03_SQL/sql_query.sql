--BEGIN create table --
CREATE TABLE IF NOT EXISTS summary (
    id INTEGER PRIMARY KEY,
    total_entries INTEGER
);
--END



--BEGIN kappa--
CREATE TABLE IF NOT EXISTS summary (
    id INTEGER PRIMARY KEY,
    total_entries INTEGER
);
--END